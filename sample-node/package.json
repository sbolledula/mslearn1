{
  "name": "sample-node",
  "version": "1.0.0",
  "description": "Small sample Node project with build script",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.js --bundle --minify --sourcemap --outfile=dist/index.js",
    "start": "node dist/index.js",
    "test": "node test/run-tests.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
